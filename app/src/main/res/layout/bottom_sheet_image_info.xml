<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingBottom="32dp">

    <!-- 拖动指示条 -->
    <View
        android:layout_width="36dp"
        android:layout_height="4dp"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="12dp"
        android:layout_marginBottom="4dp"
        android:background="@drawable/shape_drag_handle" />

    <!-- 标题 -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="图片详情"
        android:textAppearance="?attr/textAppearanceTitleMedium"
        android:textColor="?attr/colorOnSurface"
        android:paddingStart="20dp"
        android:paddingEnd="20dp"
        android:paddingTop="12dp"
        android:paddingBottom="16dp" />

    <!-- 分割线 -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="?attr/colorOutline"
        android:alpha="0.3" />

    <!-- 文件名 -->
    <LinearLayout style="@style/InfoRow">
        <TextView style="@style/InfoLabel" android:text="文件名" />
        <TextView android:id="@+id/tv_filename" style="@style/InfoValue" />
    </LinearLayout>

    <!-- 尺寸 -->
    <LinearLayout style="@style/InfoRow">
        <TextView style="@style/InfoLabel" android:text="尺寸" />
        <TextView android:id="@+id/tv_dimensions" style="@style/InfoValue" />
    </LinearLayout>

    <!-- 文件大小 -->
    <LinearLayout style="@style/InfoRow">
        <TextView style="@style/InfoLabel" android:text="大小" />
        <TextView android:id="@+id/tv_size" style="@style/InfoValue" />
    </LinearLayout>

    <!-- 拍摄时间 -->
    <LinearLayout style="@style/InfoRow">
        <TextView style="@style/InfoLabel" android:text="时间" />
        <TextView android:id="@+id/tv_date" style="@style/InfoValue" />
    </LinearLayout>

    <!-- 格式 -->
    <LinearLayout style="@style/InfoRow">
        <TextView style="@style/InfoLabel" android:text="格式" />
        <TextView android:id="@+id/tv_format" style="@style/InfoValue" />
    </LinearLayout>

    <!-- 相机品牌 -->
    <LinearLayout android:id="@+id/row_camera" style="@style/InfoRow">
        <TextView style="@style/InfoLabel" android:text="相机" />
        <TextView android:id="@+id/tv_camera" style="@style/InfoValue" />
    </LinearLayout>

    <!-- 光圈 -->
    <LinearLayout android:id="@+id/row_aperture" style="@style/InfoRow">
        <TextView style="@style/InfoLabel" android:text="光圈" />
        <TextView android:id="@+id/tv_aperture" style="@style/InfoValue" />
    </LinearLayout>

    <!-- 快门 -->
    <LinearLayout android:id="@+id/row_shutter" style="@style/InfoRow">
        <TextView style="@style/InfoLabel" android:text="快门" />
        <TextView android:id="@+id/tv_shutter" style="@style/InfoValue" />
    </LinearLayout>

    <!-- ISO -->
    <LinearLayout android:id="@+id/row_iso" style="@style/InfoRow">
        <TextView style="@style/InfoLabel" android:text="ISO" />
        <TextView android:id="@+id/tv_iso" style="@style/InfoValue" />
    </LinearLayout>

    <!-- 焦距 -->
    <LinearLayout android:id="@+id/row_focal" style="@style/InfoRow">
        <TextView style="@style/InfoLabel" android:text="焦距" />
        <TextView android:id="@+id/tv_focal" style="@style/InfoValue" />
    </LinearLayout>

    <!-- 路径 -->
    <LinearLayout style="@style/InfoRow">
        <TextView style="@style/InfoLabel" android:text="路径" />
        <TextView android:id="@+id/tv_path" style="@style/InfoValue" />
    </LinearLayout>

    <!-- GPS -->
    <LinearLayout android:id="@+id/row_gps" style="@style/InfoRow">
        <TextView style="@style/InfoLabel" android:text="位置" />
        <TextView android:id="@+id/tv_gps" style="@style/InfoValue" />
    </LinearLayout>

</LinearLayout>
